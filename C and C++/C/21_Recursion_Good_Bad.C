// For large program Its takes to much time;
// Recursion call increase exponentially;
// Some time recursiveis good approach but some time itterative better than recursive;
// Takes time because of single function call multiple times;